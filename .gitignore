node_modules
dist
*.local
.DS_Store

# uploads de imágenes (admin en runtime -> volumen)
server/uploads
server/public/imgs/prod-*

# envs
.env
.env.*
!.env.example

# logs locales
*.log

# certificados digitales (firma electrónica SRI) - nunca subir
server/certificates/

